puts "Loaded ...... customlb/lib.rb"
def adder(x, y)
    puts "#{x} + #{y} = #{x+y}"
end
  
def subtracter(x, y)
      puts "#{x} - #{y} = #{x-y}"
end
  
def multiplyer(x, y)
      puts "#{x} * #{y} = #{x*y}"
end
  
def divider(x, y)
      puts "#{x} / #{y} = #{x/y}"
end

def common
    puts
    puts "I am from customlb/lib.rb"
    puts "I am recent import"
    puts
end